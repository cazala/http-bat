/headers:
  put:
    headers:
      Authorization:
        type: string
    responses:
      200:
        headers:
          Access-Control-Allow-Headers:
            type: string
  description: Bounce the headers of the API request back to the user.
/parameter/{variable}:
  type: methods