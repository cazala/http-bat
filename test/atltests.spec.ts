declare var describe, it;

import { ATLTest } from '../lib/ATLHelpers';

// import { express } from 'express';
/*
describe('ATLTest', () => {
  it('must reject on invalid endpoint', function (done) {
    let test = new ATLTest();
    test.uri = '/asdd';
    test.run()
      .then(data => {
        done(data || true);
      })
      .catch(err => {
        done();
      });
  });

  it('must reject on invalid endpoint', function (done) {
    let test = new ATLTest();
    test.uri = '/asdd';
    test.run()
      .then(data => done(data || true))
      .catch(() => done());
  });

});*/